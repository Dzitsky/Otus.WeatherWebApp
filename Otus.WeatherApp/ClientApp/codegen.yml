overwrite: true
schema: "http://localhost:5000/graphql"
documents: "src/**/*.js(x)?"

generates:
  src/dataProviders/templateServiceTypes.ts:
    config:
      preResolveTypes: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "fragment-matcher"
